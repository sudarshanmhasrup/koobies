package compose.shared.app.presentation.system

@Suppress(names = ["EXPECT_ACTUAL_CLASSIFIERS_ARE_IN_BETA_WARNING"])
internal actual class SystemBarThemeManager actual constructor() {
    actual fun setDarkMode() {
        // No implementation needed as Desktop doesn't have navigation or status bars
    }

    actual fun setLightMode() {
        // No implementation needed as Desktop doesn't have navigation or status bars
    }
}